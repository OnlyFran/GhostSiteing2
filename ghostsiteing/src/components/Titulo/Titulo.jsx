const Titulo = ({titulo, subTitulo}) => {
    return (
        <div style={{border: "1px solid black"}}>
            <h1>{titulo}</h1>
            <h2>{subTitulo}</h2>
        </div>
    )
};

export default Titulo